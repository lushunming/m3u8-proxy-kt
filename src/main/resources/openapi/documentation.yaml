openapi: "3.1.0"
info:
  title: "m3u8_proxy_kt API"
  description: "m3u8_proxy_kt API"
  version: "1.0.0"
servers:
  - url: "http://127.0.0.1:3800"
paths:
  /:
    get:
      description: ""
      responses:
        "200":
          description: "OK"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: "Hello World!"
  /download:
    post:
      description: ""
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Download"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            text/plain:
              schema:
                type: "string"
  /proxy:
    get:
      description: ""
      parameters:
        - name: "url"
          in: "query"
          required: false
          schema:
            type: "string"
        - name: "url"
          in: "query"
          required: false
          schema:
            type: "string"
        - name: "headers"
          in: "query"
          required: false
          schema:
            type: "string"
      responses:
        "200":
          description: "OK"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: ""
  /json/gson:
    get:
      description: ""
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/Map_String"
components:
  schemas:
    Download:
      type: "object"
      properties:
        url:
          type: "string"
        headers:
          type: "string"
      required:
        - "url"
        - "headers"
    Map:
      type: "object"
      properties: { }
    Map_String:
      type: "string"